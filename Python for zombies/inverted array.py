vector = []
index = 10
while index == 1:
        number = real(input("insert the %d° number" % index )
        vector.append(number)
        index -= 1
    Print("This are inverted reads number %d" % vector)
